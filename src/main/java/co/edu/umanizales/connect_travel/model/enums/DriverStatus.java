package co.edu.umanizales.connect_travel.model.enums;

public enum DriverStatus {
    ACTIVE,     // Conductor activo y disponible
    INACTIVE,   // Conductor inactivo (cuenta deshabilitada)
    ON_LEAVE,   // Conductor en permiso o vacaciones
    SUSPENDED   // Conductor suspendido temporalmente
}
